{
  "name": "lambda-cleanup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../my-lambda-cleanup.zip . && cd .. && zip -r my-lambda-cleanup.zip node_modules",
    "clean": "rm -rf node_modules dist my-lambda-cleanup.zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.616.0",
    "@aws-sdk/node-http-handler": "^3.370.0",
    "@types/aws-lambda": "^8.10.141",
    "@types/node": "^20.14.11",
    "typescript": "^5.5.3"
  }
}
